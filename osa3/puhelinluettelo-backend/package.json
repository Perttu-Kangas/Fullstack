{
  "name": "osa3backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../puhelinluettelo-frontend/ && npm run build && cp -r build ../puhelinluettelo-backend/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && heroku git:remote -a infinite-falls-89412 && git add . && git commit -am uibuild && npm run deploy",      
    "logs:prod": "heroku logs --tail"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "morgan": "^1.10.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.18"
  },
  "proxy": "http://localhost:3001"
}
